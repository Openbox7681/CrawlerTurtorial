input {
  elasticsearch {
    hosts => "192.168.70.182:30200"
 	  index => "info_facebook"
 	  docinfo => true
  }
}

output {
  elasticsearch { hosts => ["211.23.163.51:59200"] 
   index => "sec_facebook"
    document_type => "%{[@metadata][_type]}"
    document_id => "%{[@metadata][_id]}"
   }
  stdout{
    codec => "rubydebug"
  }
  
}
